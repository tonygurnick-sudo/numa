[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.coverage.run]
omit = ["**/tests/**/*"]
source = ["."]

[tool.mypy]
python_version = "3.13"
ignore_missing_imports = true

[tool.poetry]
name = "oauth-workspace-tools"
version = "0.1.0"
description = "OAuth workspace tools for Chat V2 integration"
authors = ["Arcanum AI"]
packages = [{ include = "**/*.py" }]

[tool.poetry.dependencies]
python = "~3.13.0"
structlog = "^25.2.0"
aws-lambda-powertools = "3.9.0"
httpx = "^0.25.0"
prm = { path = "../../../lib/prm", develop = true }
oauth-providers = { path = "../../../lib/oauth-providers", develop = true }
synergy-tokenize = { path = "../../../lib/synergy-tokenize", develop = true }

[tool.poetry.group.dev.dependencies]
mypy = "1.17.1"
pylint = "3.3.8"
pyright = "1.1.403"
