[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.coverage.run]
omit = [
    "**/tests/**/*"
]
source = [
    ".",
]

[tool.poetry]
name = "aggregate-candidate-results"
version = "0.1.0"
description = "Lambda to aggregate JSON results from candidate-screening lambda"
authors = ["Sam Bentley <sam@arcanum.ai>"]
readme = "README.md"
packages = [
    { include = "**/*.py" },
]

[tool.poetry.dependencies]
python = "~3.13.0"
helpers = {path = "../../../lib/helpers", develop = true}
s3_helpers = {path = "../../../lib/s3_helpers", develop = true}
aws-lambda-powertools = "3.8.0"
structlog = "25.2.0"

[tool.poetry.group.dev.dependencies]
mypy = "1.15.0"
pylint = "3.3.6"
pyright = "1.1.397"
pre-commit = "^4.1.0"
poetry-plugin-lambda-build = "^1.0.7"
pytest = "^8.3.4"

[tool.poetry.requires-plugins]
poetry-plugin-export = ">=1.9.0"
poetry-plugin-lambda-build = ">=1.0.7"

[tool.poetry-plugin-lambda-build]
package-artifact-path = "lambda_function.zip"
without = "dev"
