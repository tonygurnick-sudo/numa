// User-facing version label shown in the UI. Update this manually when you want
// a friendly version string (e.g., after visual tweaks or minor UI changes).
// Leave empty to default to the deploy git hash.
export const MANUAL_VERSION_LABEL = 'v2.2.0';
