include:
  - project: arcanumai/ci
    file: pipelines/node.yml
  - project: arcanumai/ci
    file: pipelines/python.yml

variables:
  PYTHON_VER: "3.12"

.node_job:
  variables:
    BUILD_DIR: infra
