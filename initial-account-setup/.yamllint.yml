extends: default # https://yamllint.readthedocs.io/en/latest/configuration.html

rules:
  document-start: disable
  indentation:
    indent-sequences: consistent
  line-length: disable
  quoted-strings:
    quote-type: double
    required: false
ignore: |
  .venv
